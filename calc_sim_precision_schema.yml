# Schema for scout precisions in individual matches
#
# Example:
#
# {
#     "scout_name": "Bob",
#     "match_number": 7,
#     "team_number": "1040",
#     "alliance_color_is_red": true,
#     "sim_precision": 0.5455
# }
#


# Contains information about this schema file
schema_file:
  # Version of schema file
  # Incremented every merged schema change
  version: 1

# TODO: Add collection information to data fields once schema is implemented
# Non-calculated data
data:
  scout_name:
    type: str
  match_number:
    type: int
  team_number:
    type: str
  alliance_color_is_red:
    type: bool

calculations:
  sim_precision:
    type: float
    requires:
      unconsolidated_totals.auto_speaker: 5
      unconsolidated_totals.auto_amp: 2
      unconsolidated_totals.tele_speaker_amped: 5
      unconsolidated_totals.tele_speaker: 2
      unconsolidated_totals.tele_amp: 1
  auto_speaker_precision:
    type: float
    requires: 
      unconsolidated.auto_speaker: 5
  auto_amp_precision:
    type: float
    requires:
      unconsolidated.auto_amp: 2
  tele_speaker_amped_precision:
    type: float
    requires:
      unconsolidated.tele_speaker_amped: 5
  tele_speaker_precision:
    type: float
    requires:
      unconsolidated.tele_speaker: 2
  tele_amp_precision:
    type: float
    requires:
      unconsolidated.tele_amp: 1
